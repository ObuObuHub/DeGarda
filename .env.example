# Neon PostgreSQL Database connection
# Format: postgres://[user]:[password]@[host]/[database]?sslmode=require
# Make sure special characters in passwords are URL-encoded (@ becomes %40, etc.)
DATABASE_URL=postgres://neondb_owner:your_password@ep-example-123456.eu-central-1.aws.neon.tech/neondb?sslmode=require

# JWT Secret for authentication (use a strong random string in production)
JWT_SECRET=your-secret-key-change-in-production

# Application URL (update for production)
NEXT_PUBLIC_APP_URL=http://localhost:3000