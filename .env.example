# Database connection
# Make sure your DATABASE_URL doesn't contain special Unicode characters in the password
# If your password has special characters, ensure they are URL-encoded
# Example: password@ becomes password%40
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# JWT Secret for authentication (optional for now)
JWT_SECRET=your-secret-key-here